# Safir Scanner — Audit A-Z v2.0 / 08.09.2026

Base candidate audited: `c1bf0898baf5ca59c4480fa79ff5b58ede7d9352`
Working batch: `batch/scanner-v2-core-20260908`

Rule: this audit follows Safir World Plan A-Z v2.0. `GREEN` means current evidence exists for the stated scope. `YELLOW` means code/design exists but final evidence is missing. `RED` is a release blocker. `N/A` requires a reason.

## 1. Product / first run / main result

| Requirement | Current evidence | Status | Gap / next action |
|---|---|---:|---|
| First launch without crash, concise onboarding, guest access | Onboarding exists; scanner usable without account | YELLOW | Device clean-install evidence still required |
| Permission requested only at use | Camera requested in capture flow, not app launch | GREEN(code) | Re-test deny / don't-ask-again on device |
| Capture/import -> usable result | Camera + picker + multi-page editor + OCR + PDF library paths exist | GREEN(code) | Physical reviewer path required |
| Error states do not imitate success | Capture, import, edit, OCR and PDF failures use explicit messages | GREEN(code) | Low-storage / corrupt input evidence required |
| No placeholder/dead buttons | Current core controls map to implemented actions | YELLOW | Full visible-control walk-through required |

## 2. Scanner/OCR contract

| Feature | Status | Evidence / gap |
|---|---:|---|
| capture/import | GREEN(code) | CameraX + system picker |
| auto perspective + manual crop | GREEN(code) | OpenCV + manual crop fallback |
| multi-page | GREEN(code) | reorder/rotate/filter/delete |
| editable OCR | GREEN(code) | Processed scan pages launch bundled on-device OCR with editable text |
| PDF export | GREEN(code) | atomic PDF write; reopen-on-device still required |
| image export/share | GREEN(code) | selected processed page is copied to scoped FileProvider share export |
| text export/share | GREEN(code) | editable OCR text supports Share and TXT export |
| delete | GREEN(code) | camera-page, editor-page and PDF delete actions require confirmation where destructive |
| QR safety | N/A | QR detection is not a shipping feature in candidate 0.2.0; no QR auto-open behavior exists |
| password-protected PDF input | N/A | App does not import PDF files |

## 3. Settings / account / connectivity

- GREEN(code): Settings exposes privacy/data, camera permission, local storage, version/build/package.
- YELLOW: HTTPS public links are gated and hidden until configured; production URLs do not yet exist.
- YELLOW: Google / Email / Apple auth implementation foundation exists behind `SAFIR_AUTH_ENABLED=false` by default.
- RED external: dedicated Firebase project for `com.safir.scan`, exact Google OAuth clients/signing fingerprints, Apple Services ID/Team/Key/return URL, email delivery domain and end-to-end provider tests.
- Product rule: account remains optional for core scanning. Auth is not enabled merely because the SDK is present.
- Gmail/Drive/Calendar scopes are forbidden for identity-only sign-in.

## 4. Data / privacy / security

- GREEN(code): camera/editor/PDF/OCR document content is designed to stay on-device unless the user explicitly shares/exports it.
- GREEN(code): OCR uses a bundled on-device Latin-script model; direct OCR imports are normalized/downsampled.
- GREEN(code): no broad storage/location/contacts/mic/SMS/call-log/accessibility/overlay/background-location runtime permission is part of the product contract.
- YELLOW: final merged-manifest/dependency audit must be performed on the exact production AAB.
- RED external: production Privacy Policy, Support, developer site, and Delete Account page if accounts ship.
- RED before Play form: final Data Safety mapping must be generated from the exact SDK set after AdMob/auth production configuration is frozen.
- Never place document pages, OCR text, tokens or full auth payloads in public evidence or analytics/crash logs.

## 5. Accessibility / compatibility

- GREEN(code): capture/flash have explicit semantics; page selectors/filters expose selected state; compact Home/editor actions have larger practical targets.
- GREEN(code): detection/errors/destructive confirmation include visible text, not color alone.
- RED evidence: TalkBack output, font scaling, focus order and contrast must be tested on the exact candidate.
- RED evidence: small/large phones, background/resume, process kill/recovery, slow/no network for auth/ad modules, and low storage.

## 6. Recovery

- GREEN(code): scan draft recovery survives process restart.
- GREEN(code): recent OCR imports/share temp files are not wiped immediately on process start; stale cache is pruned after an age window.
- RED evidence: process-kill recovery must be observed on physical hardware for both scan and OCR flows.

## 7. Monetization

Business model: `FREE + ADS`.

- RevenueCat / Play Billing / IAP: N/A for this candidate because no paid digital feature is sold.
- RED: AdMob app for exact `com.safir.scan`, App ID/ad unit IDs, UMP/privacy options, test ads, no-fill behavior, app-ads.txt, Contains ads declaration and final Data Safety update.
- Placement rule: interstitial only at a natural break; never over active scanning/login confirmation or as a surprise immediately after the user requests a scan action.

## 8. Identity / CI / signing

- GREEN: repo identity is `giuni30-debug/safir-scann-document`; Android applicationId is `com.safir.scan`.
- GREEN(previous candidate): `c1bf0898...` passed lint, unit tests, debug APK, release AAB validation and 16 KB native alignment checks.
- YELLOW: current v2 batch has not yet run the full CI gate; no GREEN is claimed for its code until that single validation run finishes.
- RED: exact Play app record for `com.safir.scan` not locked/verified.
- RED: production upload key / Play App Signing relationship not verified.
- RED: Codemagic app/workflow for this exact repository/package is not yet locked.
- Rule: no platform identity from another Safir app may be reused by title similarity.

## 9. Documentation / reviewer packet

The batch now includes:
- persistent app manifest with explicit BLOCKED values;
- integration map;
- release runbook;
- incident/recovery runbook;
- scanner/OCR functional contract;
- reviewer flow including OCR/image/text/PDF actions;
- A-Z v2 release checklist.

Remaining RED documentation is production-specific: live Privacy/Support/site/delete-account URLs, exact store declarations, final signed-candidate IDs/digests and physical evidence.

## 10. One Fix Batch status

Completed in code/docs before validation:
1. integrated editable local OCR into the processed scan/editor path;
2. exposed processed-page image and editable text export/share paths;
3. hardened accessibility semantics/touch targets for key camera/editor/Home controls;
4. changed OCR/share cache cleanup from unconditional wipe to age-based pruning for recovery safety;
5. aligned README/product/reviewer/contracts with actual shipping scope;
6. changed CI so documentation-only edits do not cause unnecessary rebuilds.

Next action is exactly one full CI validation on the frozen batch. If CI finds a defect, that defect opens the next repair batch; it is not hidden by repeated builds.

## Verdict

Current state before the batch CI run: `FIXED_CODE / VALIDATION_PENDING`.

`PRODUCTION_READY` is false because physical-device, signing, exact Play identity, public URLs, auth provider configuration and AdMob gates remain open.
