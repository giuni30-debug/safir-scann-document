# Safir Scanner — Audit A-Z v2.0 / 08.09.2026

Candidate audited: `c1bf0898baf5ca59c4480fa79ff5b58ede7d9352`
Working batch: `batch/scanner-v2-core-20260908`

Rule: this audit follows Safir World Plan A-Z v2.0. `GREEN` means current evidence exists for the stated scope. `YELLOW` means code/design exists but final evidence is missing. `RED` is a release blocker. `N/A` requires a reason.

## 1. Product / first run / main result

| Requirement | Current evidence | Status | Gap / next action |
|---|---|---:|---|
| First launch without crash, concise onboarding, guest access | Onboarding exists; scanner usable without account | YELLOW | Device clean-install evidence still required |
| Permission requested only at use | Camera requested in capture flow, not app launch | GREEN | Re-test deny / don't-ask-again on device |
| Capture/import -> usable result | Camera + document picker + multi-page editor + PDF library exist | GREEN(code) | Physical reviewer path required |
| Error states do not imitate success | Capture, import, edit and PDF failures show explicit messages | GREEN(code) | Low-storage / corrupt input evidence required |
| No placeholder/dead buttons | Current core controls map to implemented actions | YELLOW | Full visible-control walk-through required |

## 2. Scanner/OCR contract

| Feature | Status | Evidence / gap |
|---|---:|---|
| capture/import | GREEN(code) | CameraX + system picker |
| auto perspective + manual crop | GREEN(code) | OpenCV + manual crop fallback |
| multi-page | GREEN(code) | reorder/rotate/filter/delete |
| editable OCR | YELLOW | Local ML Kit OCR activity exists, but is not yet integrated into the processed scan flow |
| PDF export | GREEN(code) | atomic PDF write; reopen-on-device still required |
| image export/share | YELLOW | OCR activity can share selected imported page; processed-scan first-class route still needs integration |
| text export/share | YELLOW | OCR activity supports editable text + TXT/share; scanner-flow integration still required |
| delete | GREEN(code) | page delete + PDF delete with confirmation |
| QR safety | N/A | QR detection is not a shipping feature in candidate 0.2.0; therefore there is no QR auto-open behavior. If QR is added later, URL auto-open is forbidden without a safety gate. |
| password-protected PDF input | N/A | App does not import PDF files |

## 3. Settings / account / connectivity

- GREEN(code): Settings exposes privacy/data, camera permission, local storage, version/build/package.
- YELLOW: HTTPS public links are gated and hidden until configured; production URLs do not yet exist.
- YELLOW: Google / Email / Apple auth implementation foundation exists behind `SAFIR_AUTH_ENABLED=false` by default.
- RED external: dedicated Firebase project for `com.safir.scan`, exact Google OAuth clients/SHA fingerprints, Apple Services ID/Team/Key/return URL, email delivery domain and real end-to-end provider tests.
- Product rule: account must remain optional for core scanning. Auth is not enabled merely because the SDK is present.
- Gmail/Drive/Calendar scopes are forbidden for identity-only sign-in.

## 4. Data / privacy / security

- GREEN(code): document processing is local for camera/editor/PDF and the new OCR path is designed on-device.
- GREEN(code): no broad storage/location/contacts/mic/SMS/call-log/accessibility/overlay/background-location runtime permission is part of the product contract.
- YELLOW: final merged-manifest/dependency audit must be performed on the exact production AAB.
- RED external: production Privacy Policy, Support, developer site, and Delete Account page if accounts ship.
- RED before Play form: final Data Safety mapping must be generated from the exact SDK set after AdMob/auth production configuration is frozen.
- Never place document pages, OCR text, tokens or full auth payloads in public evidence or analytics/crash logs.

## 5. Accessibility / compatibility

- RED evidence: TalkBack labels/state, 48dp practical targets, font scaling, focus order and contrast must be tested on the exact candidate.
- RED evidence: small/large phones, background/resume, process kill/recovery, slow/no network for auth/ad modules, and low storage.
- Code batch action: add explicit semantics to icon-only capture/flash/page controls and integrate OCR into the normal scan flow before the next full build.

## 6. Monetization

Business model: `FREE + ADS`.

- RevenueCat / Play Billing / IAP: N/A for this candidate because no paid digital feature is sold.
- RED: AdMob app for exact `com.safir.scan`, App ID/ad unit IDs, UMP/privacy options, test ads, no-fill behavior, app-ads.txt, Contains ads declaration, final Data Safety update.
- Placement rule: interstitial only at a natural break; never over active scanning/login confirmation or as a surprise immediately after the user requests a scan action.

## 7. Identity / CI / signing

- GREEN: repo identity is `giuni30-debug/safir-scann-document`; Android applicationId is `com.safir.scan`.
- GREEN: candidate `c1bf0898...` passed lint, unit tests, debug APK, release AAB validation and 16 KB native alignment checks in GitHub Actions.
- RED: exact Play app record for `com.safir.scan` not locked/verified.
- RED: production upload key / Play App Signing relationship not verified.
- RED: Codemagic app/workflow for this exact repository/package is not yet locked.
- Rule: no platform identity from another Safir app may be reused by title similarity.

## 8. Public docs / review packet

Existing repo docs cover product, auth, data, reviewer risks and device matrix. Missing/required in this batch:
- persistent app manifest with explicit UNKNOWN/BLOCKED values;
- integration map;
- release runbook;
- incident/recovery runbook;
- exact scanner-flow OCR integration evidence after code batch;
- final reviewer packet after AdMob/auth/public URLs are frozen.

## 9. One Fix Batch scope

No more store build after each edit. The next coordinated code batch contains only confirmed gaps that can be fixed without external account credentials:

1. Integrate editable local OCR into the processed scan/editor path.
2. Make processed-page image/text export paths directly reachable.
3. Harden accessibility semantics for icon-only / compact controls.
4. Keep local-processing truth consistent across UI/docs.
5. Update README/product/functional contract to match the actual binary.
6. Add deterministic tests for new pure policy/config helpers where practical.

After the batch is complete: one focused regression + one full CI run. Device evidence remains a separate gate and cannot be manufactured by CI.

## Verdict

`READY_FOR_INTERNAL_TEST` only after the One Fix Batch passes CI. `PRODUCTION_READY` is currently false because physical-device, signing, exact Play identity, public URLs, auth provider configuration and AdMob gates remain open.
